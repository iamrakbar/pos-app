import type { FC } from 'react';
import type { NumberPadBackspaceIconProps } from './number-pad.types';
/**
 * Default backspace icon for the NumberPad backspace key.
 * Left-pointing arrow on a 16px grid.
 * Uses the foreground theme color when no explicit color is provided.
 */
export declare const BackspaceIcon: FC<NumberPadBackspaceIconProps>;
//# sourceMappingURL=number-pad.icons.d.ts.map