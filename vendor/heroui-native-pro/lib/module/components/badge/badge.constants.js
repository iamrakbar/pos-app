"use strict";

/**
 * Display name constants for the Badge compound component parts.
 */
export const DISPLAY_NAME = {
  ROOT: 'HeroUINative.Badge.Root',
  ANCHOR: 'HeroUINative.Badge.Anchor',
  LABEL: 'HeroUINative.Badge.Label'
};

/** Default size when the `size` prop is not provided. */
export const DEFAULT_SIZE = 'md';

/** Default color when the `color` prop is not provided. */
export const DEFAULT_COLOR = 'default';

/** Default visual variant. */
export const DEFAULT_VARIANT = 'primary';

/** Default placement when inside an Anchor. */
export const DEFAULT_PLACEMENT = 'top-right';