# Copy one of these files for local variant-specific values:
#   .env.development.example -> .env.development
#   .env.preview.example -> .env.preview
#   .env.production.example -> .env.production
#
# The build scripts load .env first, then override with .env.<variant>.

EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
